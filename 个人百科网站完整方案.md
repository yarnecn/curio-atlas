# 个人百科网站与微信小程序完整方案

> 工作代号：常识地图  
> 方案日期：2026-09-02，2026-09-03 完成产品基线 1.2 方向校准  
> 一句话定位：不用搜索，打开就能学到一点的主动推荐型常识百科。

> 2026-09-07 技术决策更新：事实主库已切换为 MySQL 8.4，当前从空库开始，不保留 PostgreSQL、pgvector 或旧迁移兼容层。最新实现状态以 [接续说明](./docs/START-HERE.md) 和 [ADR-0005](./docs/adr/0005-start-clean-on-mysql.md) 为准。

> 1.2 决策：产品核心从“搜索与系统学习”校准为“打开即看的常识漫游”；即使没有用户投稿，系统也通过覆盖缺口、白名单来源、AI 候选和站长审核持续增长。候选投稿与正式常识分离，不开放评论和无审核直发。最高优先级定义见 [产品愿景与闭环](./docs/product/product-vision-and-loops.md)。

## 1. 产品结论

这个项目不是依赖用户先搜索的词条仓库，而应定位为：

**面向成年人的、可以随手翻的主动推荐型常识百科大全。**

产品由四部分构成：

1. 打开即可阅读、跨领域发现的常识流；
2. 有结构、有来源、可持续更新的公共常识百科；
3. 能把现实事件连接到基础知识的背景解释；
4. 可选的学习路径、收藏同步和个人沉淀。

核心竞争力不是词条数量，而是：

- 知道用户不知道该搜什么时，下一条值得看什么；
- 知道知识之间如何连接；
- 即使没有用户投稿也能持续发现缺口和更新内容；
- 所有事实有来源、时间和版本；
- 热点出现时，能解释其背后的历史、制度和原理；
- 复杂能力不妨碍访客无登录地随手浏览。

## 2. 目标用户与核心场景

### 2.1 首批目标用户

- 20—45 岁、已经工作，希望系统补充常识的人；
- 学历基础一般，但有持续学习意愿的人；
- 知道很多零散信息，却无法形成整体框架的人；
- 希望理解政策、经济、技术和国际事件背景的人；
- 不愿意实名发言，但愿意安静获取知识的人。

首版不针对应试学生，也不做专业学历教育。

### 2.2 核心用户循环

```text
打开即看到一条常识
   ↓
换一条 / 继续了解
   ↓
发现新的兴趣和知识关系
   ↓
收藏、有用或沿关系深入
   ↓
下次推荐更合适，同时保留跨领域探索
```

北极星指标使用“每周有收获浏览会话数”，不使用搜索量、注册数或单纯浏览量。系统学习和复习是可选的深度闭环，不是进入产品的前提。

## 3. 产品范围与版本边界

### 3.1 完整产品能力

- 跨学科知识树；
- 概念、人物、地点、事件、制度、公式、术语等多类型知识节点；
- 时间线、地图、关系网络和对比表；
- 学习路径、诊断、自测、错题和间隔复习；
- 全文、筛选、关系和语义混合搜索；
- 带来源引用的 AI 问答；
- 个人笔记、收藏、标注和知识导出；
- 可信来源订阅、变更检测和编辑审核；
- 每日知识卡与每周认知简报；
- 网站、微信小程序和管理后台共用一套数据。

### 3.2 MVP 必须包含

- 无需登录即可浏览公共知识；
- 12 个一级知识领域和树形目录；
- 知识节点、版本、来源和关系；
- 三条完整学习路径；
- 搜索、收藏、学习进度、题目和复习；
- 内容编辑、审核和发布后台；
- 网站端候选精华投稿、AI 预处理和“有用 / 没用”尝试展示；
- 小程序的今日学习、复习、搜索和进度；
- 少量官方数据源的定时采集及人工审核；
- 个人数据 Markdown/JSON 导出。

### 3.3 MVP 明确不做

- 不开放无审核直发、公开评论、回复、私信和自由编辑正式常识；
- 不把用户票数、热点热度或 AI 置信度当作事实证明；
- 不追求覆盖所有学科；
- 不建设独立新闻采编系统；
- 不让 AI 自动发布事实性内容；
- 不上微服务、Kafka、Neo4j 等重型基础设施；
- 不先做复杂社交、排行榜和积分商城；
- 不复制百科、教材或新闻全文。

## 4. 首版知识范围

技术架构支持全部领域，首版内容采用“广度优先、单点闭环”，避免网站看起来只有少数几门学科。

建议首批公开测试内容为 150—250 个知识节点、300—500 道题：

| 一级领域 | 稳定节点数 | 覆盖 |
|---|---:|---|
| 地理 | 20 | 空间、区域、资源与地球系统 |
| 历史与文明 | 20 | 证据、文明演变与现代转型 |
| 政治法律与社会制度 | 15 | 政府、法律、公共服务与协作 |
| 经济金融 | 15 | 经济运行、个人财务、商业与就业 |
| 数学统计与逻辑 | 12 | 数字、概率、推理与数据阅读 |
| 物理化学生物 | 18 | 物质、生命、地球、宇宙与环境 |
| 健康与生活风险 | 15 | 身体、习惯、心理、急救与安全 |
| 计算机与数字安全 | 12 | 互联网、隐私、安全、AI 与数据 |
| 工程与基础设施 | 10 | 建筑、能源、交通、通信与城市系统 |
| 语言表达与沟通 | 10 | 阅读、写作、论证、沟通与媒介 |
| 文化艺术与日常生活 | 10 | 文学、艺术、影像与生活文化 |
| 时事背景与世界格局 | 8 | 国家、国际组织、指标与事件背景 |

另保留 20 个动态热点背景名额，不计入 165 条稳定内容完成率。12 个领域继续拆为 48 个细分话题，机器可读目录以 `content/v1/catalog.json` 为准。

长期可以规划一套“成年人 1,000 个核心知识节点”，再逐渐扩展到 3,000—5,000 个节点。

## 5. 信息架构

### 5.1 网站

网站承担公开传播、深度阅读、SEO、复杂可视化和内容管理。

```text
首页
├─ 随便看看（默认首屏常识卡）
├─ 今天值得知道
├─ 你可能感兴趣
├─ 沿着这个继续
├─ 知识地图
│  ├─ 地理
│  ├─ 历史与文明
│  ├─ 政治法律与社会制度
│  ├─ 经济金融
│  ├─ 数学统计与逻辑
│  ├─ 物理化学生物
│  ├─ 健康与生活风险
│  ├─ 计算机与数字安全
│  ├─ 工程与基础设施
│  ├─ 语言表达与沟通
│  ├─ 文化艺术与日常生活
│  └─ 时事背景与世界格局
├─ 学习路径
├─ 时间线
├─ 地图
├─ 事件背景
├─ 候选精华
│  ├─ 小流量尝试
│  └─ 有用 / 没用
├─ 我要贡献
├─ 搜索 / AI 问答（辅助入口）
├─ 我的知识
│  ├─ 掌握地图
│  ├─ 收藏与笔记
│  ├─ 错题与复习
│  └─ 数据导出
└─ 管理后台
```

建议公开路由：

```text
/
/map
/domains/:slug
/knowledge/:slug
/paths/:slug
/timeline
/events/:slug
/submissions
/contribute
/search
/ask
/me/*
/admin/*
```

### 5.2 微信小程序

小程序承担高频、轻量、碎片化使用，不承载复杂编辑器。

底部四个 Tab：

1. **今天**：打开即看的每日常识和现实背景；
2. **随便看**：跨领域连续浏览、换一条和关系延伸；
3. **知识领域**：目录、兴趣和可选学习路径；
4. **我的**：收藏、已读、进度和设置；搜索放在顶部辅助入口。

网站与小程序共用账号、收藏、进度、复习记录和 API。

首版完整投稿编辑器只在网站实现；小程序提供阅读、搜索、有用 / 没用、收藏和学习，以控制包体、开发量与维护成本。

## 6. 知识内容模型

### 6.1 知识节点类型

统一使用 `knowledge_node`，通过 `type` 区分：

- `concept`：概念；
- `person`：人物；
- `place`：地点或行政区；
- `event`：历史或现实事件；
- `institution`：机构或制度；
- `formula`：公式或定理；
- `term`：术语；
- `phrase`：语言短语；
- `law_policy`：法律或政策；
- `dataset_indicator`：统计指标。

节点至少包含：

```text
id
type
title
aliases
slug
domain_id
summary
difficulty
stability_level
status
current_revision_id
created_at
updated_at
next_review_at
```

### 6.2 内容版本

正式内容不能直接覆盖。每次修改创建不可变的 `knowledge_revision`：

```text
id
knowledge_node_id
version
content_blocks
change_summary
author_id
reviewer_id
review_status
created_at
published_at
```

正文采用结构化 Block JSON，支持：

- 标题与正文；
- 图片；
- 数学公式；
- 时间线；
- 地图；
- 对比表；
- 例子与反例；
- 常见误解；
- 引用来源；
- 互动题目。

同时提供 Markdown 导出。这样网站和小程序可以分别实现渲染器，又能保证用户长期拥有可读文件。

### 6.3 事实与来源

重要事实拆成 `claim`，而不是只给整篇文章挂一个参考链接：

```text
claim
├─ statement
├─ confidence
├─ valid_from / valid_to
├─ source_id
├─ evidence_locator
├─ verified_by
└─ verified_at
```

`source` 保存：

```text
publisher
title
url
source_type
published_at
accessed_at
license
language
checksum
```

### 6.4 知识关系

使用普通关系表即可，首版不引入图数据库：

```text
knowledge_relation
├─ from_node_id
├─ to_node_id
├─ relation_type
├─ strength
├─ valid_from / valid_to
└─ evidence_source_id
```

关系类型示例：

- `prerequisite_of`：是……的前置知识；
- `part_of`：属于；
- `causes`：导致；
- `influences`：影响；
- `contrasts_with`：对比；
- `located_in`：位于；
- `occurred_during`：发生于；
- `succeeded_by`：被……继承；
- `explains`：解释某个现实事件；
- `related_to`：一般关联。

### 6.5 学习数据

核心表：

- `learning_path`、`path_unit`：学习路径；
- `question`、`question_option`：题目；
- `user_attempt`：答题记录；
- `user_mastery`：用户对节点的掌握程度；
- `review_schedule`：下次复习时间；
- `collection`、`bookmark`、`user_note`：个人沉淀；
- `user_export_job`：数据导出。

掌握度分为四级即可：

1. 见过；
2. 能识别；
3. 能解释；
4. 能应用并连接其他知识。

## 7. 内容生产标准

用户贡献先进入独立的候选精华状态机，不直接写入正式节点：

```text
submitted → automated_screening → trial
          → expanded_trial → queued_for_review
          → merged / rejected / withdrawn
```

冷启动默认门槛是：自动检查通过后进入小流量尝试；有用数至少 5、净有用数至少 4 且有用率至少 70% 时扩大展示；至少 20 个有效评分且有用率至少 80% 时进入人工审核。票数只决定是否值得审核，不决定事实真假。

系统内部编辑候选走另一条不依赖用户票数的路径：

```text
覆盖缺口 / 来源发现 / 到期维护
          → automated_screening → queued_for_review
          → draft / merged / rejected / held
```

所有候选记录 `origin_type`。用户投稿才进入公开尝试池；内部编辑候选按覆盖价值、来源变化和风险排序后直接交给站长审核。两条路径都不能绕过人工发布。

每个基础知识节点采用统一模板：

1. 一句话解释；
2. 为什么值得知道；
3. 核心事实；
4. 背后的原理或背景；
5. 一个现实例子；
6. 常见误解；
7. 前置和后续知识；
8. 2—5 个自测问题；
9. 来源、发布日期和复核日期。

事件节点采用另一套模板：

1. 发生了什么；
2. 已确认事实；
3. 时间线；
4. 相关主体；
5. 需要理解的基础概念；
6. 各方公开立场；
7. 分析与预测，明确标记为观点；
8. 尚不确定的信息；
9. 多来源引用。

内容状态：

```text
draft → pending_review → approved → published → superseded/archived
```

AI 只能产生 `draft` 或 `pending_review`，不能直接进入 `published`。

## 8. 定时采集与编辑流水线

### 8.1 来源策略

建立白名单，不做无边界全网爬取：

1. 法律、政策和政府原文；
2. 国家统计和部门公开数据；
3. 联合国、世界银行等国际组织开放数据；
4. 大学、科研机构和开放教材；
5. 获授权媒体或公开 RSS；
6. 其他媒体只作为发现线索，是否保存正文取决于许可。

### 8.2 处理流程

```text
RSS / API / 允许采集的网页
          ↓
       Fetcher
          ↓
保存元数据、原始快照或内容指纹
          ↓
正文抽取、格式统一、语言识别
          ↓
URL + 内容哈希 + 标题相似度去重
          ↓
AI 分类、摘要、事实提取、实体连接
          ↓
与现有 claim 对比，标记新增/冲突/过期
          ↓
进入编辑审核队列
          ↓
人工确认来源和表述
          ↓
发布新节点或新版本
          ↓
更新搜索索引、向量、简报和复习材料
```

### 8.3 建议调度

- 官方政策与公告：每日多次检查；
- 统计数据：每日检查，按实际发布周期更新；
- 国际组织数据：每周检查；
- 来源失效和链接检查：每周；
- 知识过期扫描：每月；
- 每周认知简报：固定一周一次；
- 全量重新生成向量：仅模型或分块规则变化时进行。

抓取任务必须支持：

- ETag、Last-Modified 和增量抓取；
- 限速、重试、指数退避和熔断；
- 幂等；
- 失败队列；
- 运行审计；
- 来源停用开关；
- 版权与 robots/服务条款配置。

## 9. 搜索与 AI 问答

### 9.1 搜索分三阶段

第一阶段：

- 标题、别名、拼音或关键词匹配；
- 领域、类型、时间和难度筛选；
- 首版使用 MySQL 标题、别名和关键词索引；数据证明需要后再评估独立搜索服务。

第二阶段：

- 使用可替换的向量检索服务增加语义召回，不与事实主库绑定；
- 对标题精确命中、别名、前置关系和语义相似度加权；
- 事件搜索额外考虑新鲜度。

第三阶段：

- 数据量和并发足够大后，再增加 OpenSearch 等专用搜索服务；
- 不在 MVP 提前承担独立搜索集群的运维成本。

### 9.2 AI 问答规则

AI 回答流程：

```text
问题理解
  ↓
关键词 + 语义 + 关系混合检索
  ↓
只读取已发布版本和允许使用的来源
  ↓
生成带句级引用的回答
  ↓
检查引用是否真的支持结论
  ↓
置信度不足时明确回答“资料库中暂时没有足够依据”
```

必须具备：

- 模型供应商抽象层，避免绑定单一模型；
- Prompt 和模型版本记录；
- 每个回答保存检索片段与引用；
- 敏感信息和内容安全检查；
- 请求限流、配额和成本统计；
- 用户反馈与纠错入口；
- 固定评测集，持续测试正确性和引用覆盖率。

首版公开 AI 问答应当晚于基础内容、来源体系和审核后台。前期 AI 优先作为内部编辑助手。

## 10. 推荐技术架构

### 10.1 架构原则

- TypeScript 全栈，降低多端协作成本；
- 模块化单体，暂不微服务化；
- 网站与小程序共享 API、类型和设计变量，不强求共享全部 UI；
- MySQL 8.4 是事实主库；
- 异步任务进入队列；
- 所有外部服务通过 Adapter 封装；
- 内容和 AI 都可审计、可回滚。

### 10.2 技术栈

| 层 | 推荐方案 |
|---|---|
| Monorepo | pnpm workspace + Turborepo |
| 网站 | Next.js App Router + React + TypeScript |
| 小程序 | Taro + React + TypeScript，编译微信小程序 |
| API | NestJS REST API + OpenAPI |
| 后台任务 | NestJS standalone worker + BullMQ |
| 主数据库 | MySQL 8.4 |
| 语义检索 | 首版不启用；需要时接入独立适配器 |
| 队列与缓存 | Redis |
| 文件 | 有实际图片或附件需求后再接对象存储 |
| 编辑器 | 自定义 Block Schema，首版使用轻量 Block Editor |
| 可观测性 | 结构化日志、错误追踪、指标和任务运行记录 |
| 本地环境 | Docker Compose |
| 部署 | Docker 镜像 + 中国大陆云主机/容器服务 |

Next.js、NestJS、MySQL 与 Redis 先按单镜像应用加外部数据服务部署。首版不引入向量数据库、对象存储或搜索集群，出现真实需求和数据规模后再通过 Adapter 接入。

### 10.3 系统图

```text
┌────────────┐   ┌──────────────┐   ┌────────────┐
│  Next.js   │   │ 微信小程序    │   │ 管理后台    │
│  公开网站   │   │ Taro/React   │   │ Next.js    │
└─────┬──────┘   └──────┬───────┘   └─────┬──────┘
      └─────────────────┼───────────────────┘
                        ↓
              ┌──────────────────┐
              │ NestJS REST API  │
              ├──────────────────┤
              │ Auth / Knowledge │
              │ Learning / Search│
              │ Source / AI      │
              │ Admin / Export   │
              └──────┬─────┬─────┘
                     │     │
              ┌──────▼─┐ ┌─▼────────┐
              │ MySQL  │ │ Redis    │
              │  8.4   │ │ BullMQ   │
              └──────┬─┘ └─┬────────┘
                     │     │
              ┌──────▼─────▼────────┐
              │ Worker / Scheduler  │
              │ 抓取、AI、索引、简报 │
              └──────┬──────────────┘
                     ↓
           官方 API / RSS / LLM / 对象存储
```

### 10.4 仓库结构

```text
knowledge-map/
├─ apps/
│  ├─ web/                 # 公开网站、个人中心、首版管理后台
│  ├─ miniprogram/         # Taro 微信小程序
│  ├─ api/                 # NestJS API
│  └─ worker/              # 抓取、索引、AI 和定时任务
├─ packages/
│  ├─ contracts/           # DTO、OpenAPI 生成类型、错误码
│  ├─ content-schema/      # 内容 Block、节点和关系定义
│  ├─ domain/              # 可共享的纯业务规则
│  ├─ api-client/          # Web/小程序共享客户端
│  ├─ design-tokens/       # 色彩、字号、间距等，不强共享组件
│  ├─ config/              # ESLint、TypeScript、测试配置
│  └─ test-fixtures/
├─ infra/
│  ├─ docker/
│  ├─ migrations/
│  └─ deployment/
├─ docs/
│  ├─ product/
│  ├─ architecture/
│  ├─ adr/
│  └─ content-guidelines/
└─ docker-compose.yml
```

### 10.5 API 模块

- `auth`：用户与外部身份；
- `taxonomy`：领域和目录；
- `knowledge`：节点、版本和关系；
- `sources`：来源与事实引用；
- `learning`：路径、题目、掌握度和复习；
- `search`：关键词与语义搜索；
- `events`：现实事件和背景连接；
- `ingestion`：来源、抓取和审核队列；
- `ai`：模型适配、RAG、评测和成本；
- `admin`：审核、发布、审计和权限；
- `export`：用户数据导出；
- `notifications`：订阅和小程序通知。

## 11. 账号与小程序设计

账号采用“按动作逐级增加身份”的方式。阅读、换一条、临时兴趣和设备内已读不登录；云端收藏、同步和有效投票使用轻量登录但不公开实名；用户第一次提交公开候选时再完成依法需要的后台真实身份认证。投稿者可公开昵称或不公开署名，认证信息与公开资料分离并最小化保存。

数据库使用统一 `user`，身份单独存放：

```text
user_identity
├─ user_id
├─ provider       # wechat_miniprogram / email / phone / web_wechat
├─ provider_subject
└─ metadata
```

小程序通过 `wx.login` 获取临时凭证，发送给服务端换取微信身份，再由自己的服务端签发登录态。微信会话密钥只保留在服务端，不下发客户端。

小程序请求使用 HTTPS API 域名，并在小程序后台配置合法通讯域名。建议域名划分：

```text
www.example.com      网站
api.example.com      API
static.example.com   图片与静态资源
```

浏览基础知识不强制登录；先把已读和临时兴趣保存在设备，只有云端收藏、跨端进度和笔记等需要同步时再登录。尽量不收集手机号、位置、通讯录等与核心功能无关的信息。

## 12. 安全、质量与运维

### 12.1 安全

- 管理后台使用 RBAC 和多因素认证；
- Refresh Token 哈希保存并支持撤销；
- 所有发布、审核和模型调用有审计日志；
- 外部 URL 抓取防止 SSRF；
- HTML 清洗，防止 XSS；
- API 限流、验证码和防批量爬取；
- 私人笔记与公共内容严格区分权限；
- 定期数据库备份并验证恢复；
- 密钥只通过环境变量或密钥管理系统注入。

### 12.2 测试

- 领域规则单元测试；
- API 集成和契约测试；
- 网站 E2E 测试；
- 小程序关键流程自动化或回归清单；
- 数据迁移测试；
- 内容校验：缺来源、死链接、过期、孤立节点；
- AI 评测：事实正确性、引用支持度、拒答和敏感内容。

### 12.3 CI/CD

每次提交执行：

```text
lint → typecheck → unit test → API contract test
→ content lint → migration check → build → security scan
```

发布流程：

```text
构建不可变镜像 → 数据库迁移 → 灰度部署
→ 健康检查 → 冒烟测试 → 切流 → 可回滚
```

## 13. 合规边界

以下是产品设计约束，不是正式法律意见，公开上线前应根据经营主体、服务器所在地、收费方式和实际功能进行专项确认。

### 13.1 网站和小程序备案

- 中国大陆服务器上的网站需要办理相应 ICP 备案；
- 小程序属于 APP 备案覆盖范围，应通过分发平台履行备案；
- 如果选择“教育”等可能存在前置审批或特殊资质的分类，要先核对主体条件；
- 小程序中应展示备案号和隐私规则。

### 13.2 个人信息

- 遵循合法、正当、必要和最小化原则；
- 明确列出账号、学习记录、收藏和笔记的用途、保存期限和删除方式；
- 用户可以注销账号并导出/删除个人数据；
- 调用微信隐私相关接口前完成隐私保护指引和同意流程；
- 未成年人功能如果不是首版目标，应避免不必要地收集其信息。

### 13.3 新闻与热点

通过网站、小程序等向公众提供互联网新闻信息服务涉及许可要求。因此首版应定位为：

- 基础知识；
- 官方文件和数据的来源导航；
- 历史背景和概念解释；
- 不自行采编突发新闻；
- 不建立开放新闻发布平台；
- 对时效内容标注来源、时间和“事实/观点/预测”。

热点功能上线前，需要就“采集、编辑、制作、转载、传播平台”中的实际形态进行法律合规评估。

### 13.4 地图

- 使用依法审核批准、带审图号的底图或合规地图服务；
- 不自行绘制和公开存在版图风险的地图；
- 行政区划变化以权威来源为准；
- 如提供定位、用户标注或地图数据库开发等能力，要进一步确认测绘资质和数据存储要求。

### 13.5 AI 与版权

- 面向境内公众提供生成式内容，需要评估生成式人工智能服务相关义务；
- 优先选择已经完成相应合规工作的模型/API 服务；
- AI 生成内容显著标识，保留反馈与投诉入口；
- 不把未经授权的教材、书籍、付费文章和新闻全文永久入库；
- 对来源记录许可类型，公开内容优先使用开放许可、公共领域和获得授权的材料；
- 摘要不能替代版权判断，爬取必须遵守 robots、服务条款和合理访问频率。

AI 管理员可以自动做来源检查、去重、分类、措辞压缩、过期检测、成本和性能巡检，并创建草稿或审批请求；正式发布、撤回、合并、高风险内容、阈值和白名单变化、生产部署仍由人工确认。具体见 [AI 管理员方案](./docs/architecture/ai-operator.md)。

版权与专利按阶段投入：来源许可、投稿授权、隐私和 AI 标识必须在公开上线前完成；商标、软件著作权和系统化维权在出现真实留存后推进；可能申请专利的具体技术方案要在公开披露前评估。具体见 [上线、运营合规与知识产权计划](./docs/operations/launch-compliance-and-ip.md)。

## 14. 商业模式

第一阶段先验证“用户愿意持续随手看，并且每次都有收获”，不急于广告变现。

免费层：

- 公共知识地图；
- 常识流、关系探索和基础学习路径；
- 每日知识、来源和纠错；
- 普通搜索。

会员层可考虑：

- 跨端同步、个性化知识清单和离线包；
- 更完整的学习路径、复习与统计；
- 带引用 AI 问答额度；
- 个人知识库、批量导入和导出；
- 专题简报与深度路径；
- 多端同步。

长期方向：企业通识培训、行业知识地图、学校或机构定制内容。广告或赞助最后评估，不能影响事实排序、审核结论或跨领域探索位。基础常识阅读保持免费，不出售用户隐私。

## 15. 指标体系

### 产品指标

- 首次访问完成 3 条有效阅读的比例；
- 每周有收获浏览会话数；
- 7 日内再次浏览的比例；
- 从常识卡继续了解或收藏的比例；
- 复习完成率；
- 学习路径完成率；
- 每百条推荐的跨领域覆盖和重复率；
- 收藏转化为学习或笔记的比例。

### 内容质量指标

- 已发布节点来源覆盖率；
- 重要 claim 的引用覆盖率；
- 来源失效和内容过期率；
- 用户纠错数量和平均处理时间；
- AI 回答引用支持度；
- AI 低证据问题的正确拒答率。

### 技术指标

- API 可用性和 P95 延迟；
- 抓取任务成功率；
- 队列积压；
- 搜索成功率；
- 每次 AI 回答成本；
- 备份恢复演练结果。

## 16. 实施路线图

以下时间按一名有经验开发者全职或 AI 辅助开发估算；兼职实施应按实际投入扩展。

### Phase 0：产品与工程骨架，2—4 天

- 确认工作名称和首批用户；
- 写 PRD、内容规范和架构决策记录；
- 创建 Monorepo；
- 建立 Next.js、Taro、NestJS、MySQL、Redis；
- 建立 CI、环境配置、健康检查和本地一键启动。

验收：新开发者按照 README 能在本地启动网站、API、worker 和数据库。

### Phase 1：内容内核，1—2 周

- 建立 MySQL 空库结构和幂等初始化；
- 实现领域、兴趣、话题、实时信息、热点、候选、评分、节点、版本、来源、claim 和关系；
- 实现候选状态机、投票有效性、AI 预处理占位和晋级规则；
- 实现网站最小投稿页与管理后台草稿、合并、审核、发布和回滚；
- 手工录入 20—30 个正式节点和 10—20 条候选样例。

验收：任何正式内容都有版本、来源和审核记录，候选与 AI 都不能绕过人工发布。

### Phase 2：公开网站，1—2 周

- 打开即看的首页常识流、换一条和已读去重；
- 知识目录、节点详情、关系导航和时间线基础组件；
- 编辑精选、领域轮换、多样性和低成本预计算推荐；
- 基础搜索和过滤，作为辅助入口；
- 最小覆盖缺口扫描、来源更新检查和站长审核包；
- SEO、站点地图、分享图和响应式布局。

验收：访客无需登录或搜索，打开首页即可连续获得有价值且不重复的常识，并能沿关系继续阅读和查看来源；没有投稿时系统仍能生成待审核内容任务。

### Phase 3：自动化内容运营，1—2 周

- 常识覆盖地图、空白和薄弱项扫描；
- 来源白名单、RSS/API/允许网页的增量检查；
- 去重、变更检测、关系断点和复核到期任务；
- AI 辅助生成候选、摘要、claim、差异和风险标记；
- 按新增、补充、冲突、过期和下架建议组织批量审核；
- 运行记录、失败重试、预算和人工采用率报告。

验收：没有外部用户时，系统也能持续生成高质量审核包；所有外部信息和 AI 结果都不能绕过站长进入正式发布态。

### Phase 4：微信小程序，1—2 周

- 今日、随便看、知识领域和我的四个入口；
- 无登录浏览、设备内已读与兴趣，按需触发微信登录和跨端同步；
- 节点阅读、关系延伸、有用 / 没用、收藏和辅助搜索；
- 合法域名、隐私指引、备案和提审准备；
- 弱网、包体和真机测试。

验收：用户首次打开小程序无需登录即可开始浏览；登录后网站与小程序收藏和已读进度同步。

### Phase 5：个性化与可选学习系统，1—2 周

- 显式兴趣、收藏、已读、继续了解和有用反馈驱动的推荐；
- 学习路径、题目与答题；
- 掌握度与间隔复习；
- 收藏、笔记、个人进度和数据导出；
- 第一版个性化每周简报。

验收：不登录的常识漫游仍完整可用；愿意登录的用户可以完成跨端沉淀和“浏览—深入—复习”的增强闭环。

### Phase 6：公开测试与变现验证，约 1—2 周

- 150—250 个高质量正式节点和稳定常识流；
- 备案、隐私、用户协议、投稿认证和内容纠错入口；
- 性能、安全、备份恢复、监控和内容运营演练；
- 邀请 20—50 名种子用户，验证有收获会话和回访；
- 连续留存达标后，小范围测试会员增强或高价值专题，不先上复杂广告系统。

验收：产品在“只有站长”和“出现真实用户”两种情况下都能闭环运行，付费实验不影响基础百科可信度。

### Phase 7：带来源 AI 问答，可选且最后

- 文本分块和可替换的向量检索适配器；
- 混合检索；
- 引用生成与验证；
- 敏感内容、限流和成本控制；
- 至少 50—100 个固定评测问题。

验收：回答可以定位到已发布节点和具体来源；证据不足时拒答。

总体：全职约 8—12 周形成可公开测试的 MVP；兼职更现实的周期是 3—5 个月。最大的工作量通常不是编码，而是常识覆盖设计、来源审核和持续运营。

## 17. 关键风险与应对

| 风险 | 应对 |
|---|---|
| 知识范围无限膨胀 | 用覆盖地图分层扩展，首版只生产 3—5 个领域的高质量常识流 |
| 开发替代学习和内容验证 | 先录入 20 个真实节点，再决定数据库和编辑器细节 |
| AI 幻觉污染百科 | AI 不得自动发布；claim、引用和固定评测集三道防线 |
| 抓取内容侵权 | 白名单、许可字段、仅存必要元数据、正文授权策略 |
| 热点演变成新闻业务 | 首版仅做背景知识与来源导航，上线前专项合规评估 |
| 地图版图错误 | 使用带审图号服务，不自制公开底图 |
| 小程序审核受阻 | 提前确认主体、服务类目、备案、隐私和内容范围 |
| 搜索架构过重 | MySQL 先满足事实存储，出现真实检索瓶颈再接独立搜索或向量服务 |
| 个人数据被平台锁死 | 从第一版提供 Markdown/JSON 导出 |
| 运营成本不可控 | 模型配额、缓存、异步生成、成本仪表盘和会员额度 |

## 18. 最正确的开发顺序

不要先写爬虫，也不要先做 AI 聊天页面。建议严格按以下顺序：

1. 审核 Phase 1 产品准入基线；
2. 手工写 20 个正式节点和 10—20 条候选精华；
3. 用真实内容验证分层、状态机和审核结构；
4. 建数据库、版本、来源、投票和关系；
5. 做网站投稿与管理审核闭环；
6. 做打开即看的公开常识流和关系探索；
7. 立即补最小覆盖缺口、来源检查和 AI 审核包，保证无用户也能增长；
8. 完整化自动内容运营，再做小程序的轻量高频浏览；
9. 增加个性化、收藏同步和可选学习系统；
10. 验证留存与变现，最后再决定是否接入公开 AI 问答。

原因很简单：内容模型没稳定之前，抓得越多，后续清洗和迁移成本越高。

## 19. 切换到项目目录后的第一轮任务

进入真实项目后，第一轮只做 Phase 0，不直接堆功能：

1. 检查现有目录、技术栈和 Git 状态；
2. 创建 `docs/product/mvp.md`；
3. 创建 `docs/architecture/overview.md`；
4. 创建第一份 ADR，确认 TypeScript Monorepo 和模块化单体；
5. 搭建四个应用和共享包；
6. 连接本地 MySQL 与 Redis，不预装暂未使用的对象存储；
7. 打通 `/health`；
8. 建立 lint、typecheck、test 和 build；
9. 只提交一个可运行的最小骨架。

开始实施时可以直接使用这句话：

> 按《个人百科网站与微信小程序完整方案》的 Phase 0 开始。先检查当前项目和约束，再搭建可运行的最小工程骨架，不实现业务功能。

## 20. 当前技术与合规参考

- [Next.js App Router 官方文档](https://nextjs.org/docs/app)
- [Next.js 部署方式](https://nextjs.org/docs/app/getting-started/deploying)
- [NestJS BullMQ 队列](https://docs.nestjs.com/techniques/queues)
- [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)
- [Taro 文档](https://docs.taro.zone/docs/taroize)
- [工信部关于开展移动互联网应用程序备案工作的通知](https://www.gov.cn/zhengce/zhengceku/202308/content_6897341.htm)
- [中华人民共和国个人信息保护法](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm)
- [互联网用户账号信息管理规定](https://www.cac.gov.cn/2022-06/26/c_1657868775042841.htm)
- [互联网新闻信息服务许可管理实施细则](https://www.cac.gov.cn/2017-05/22/c_1121015789.htm)
- [地图管理条例](https://www.gov.cn/zhengce/zhengceku/2015-12/14/content_10403.htm)
- [生成式人工智能服务管理暂行办法](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)
- [人工智能生成合成内容标识办法](https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm)
- [网络出版服务管理规定](https://www.nppa.gov.cn/xxgk/fdzdgknr/zcfg_210/bmgz_213/202112/P020221115748304315015.pdf)
